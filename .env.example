# Reference only. This file is not loaded automatically.
# Set variables in PowerShell before exporting; never commit real credentials.
PGHOST=
PGPORT=5432
PGDATABASE=
PGUSER=
# Leave PGPASSWORD unset to use the hidden password prompt.
# STOCK_DB_PATH defaults to data/demo/stocks.sqlite
# OLLAMA_URL defaults to http://127.0.0.1:11434
# OLLAMA_MODEL defaults to stock-agent:4b (see models/Modelfile)
